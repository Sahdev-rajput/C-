#include <iostream>
#include <set>
using namespace std;
int main()
{
	set<int> v = { 10,20,30,40,50,60 };
	v.insert(70);
	v.insert(80);
	v.insert(90);
	cout << "Using for each loop accessing the element of array:"<<endl;
	for (int x : v) {
		cout << x<<endl;
	}
	cout << endl;
	cout << "Using iterators accessing the value of elements of array"<<endl;
	cout << "Lets check whether it will modify the array or not" << endl;
	for (int c : v)
	{
		cout << c << endl;
	}
}
